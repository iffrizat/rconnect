<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RCONnect"
        tools:targetApi="35">
        <activity
            android:name=".RconConnectionActivity"
            android:exported="false"
            android:label="@string/title_activity_rcon_connection"
            android:theme="@style/Theme.RCONnect" />
        <activity
            android:name=".RconConfigEditorActivity"
            android:exported="false"
            android:label="@string/title_activity_rcon_config_editor"
            android:theme="@style/Theme.RCONnect" />
        <activity
            android:name=".RconConfigPickerActivity"
            android:exported="true"
            android:theme="@style/Theme.RCONnect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>